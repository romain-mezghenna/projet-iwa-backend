# Nom du service
spring.application.name=reservations-service

# Port du service
server.port=8081

# Configuration de la base de données (PostgreSQL par exemple)
spring.datasource.url=jdbc:postgresql://localhost:5432/reservation_service
spring.datasource.username=postgres
spring.datasource.password=Robin0230

# Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Activer la découverte de services (si tu utilises Eureka)
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

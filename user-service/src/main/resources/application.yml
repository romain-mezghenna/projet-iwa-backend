spring:
  application:
    name: user-service

  kafka:
    bootstrap-servers: kafka:9092
    consumer:
      group-id: user-service-group

# Variables d'environnement par d�faut (admin IDs)
admin:
  ids: ${ADMIN_IDS:1,2,3}